/**
 * Created by liub on 2019.06.05
 */
function handle(data){ 
    //todo 用户账户发生变动，对应服务端的 balance.account.client, 设法通知客户端
    //data.user.notfiy('balance.account.client', data.msg);
}

module.exports.handle = handle;